services:
  brewbot:
    build:
      context: .
      args:
        VERSION: ${VERSION:-dev}
    container_name: brewbot
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/data/brewbot.db
    volumes:
      - ./data:/data
